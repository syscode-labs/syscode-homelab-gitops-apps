# Storage for homelab-kvm clusters (unraid-lab). Cloud clusters use their provider's
# CSI instead (per-type storage) — see values/types/cloud. Longhorn needs the Talos
# iscsi-tools + util-linux-tools extensions, which the syscode talos image already ships.
chart: longhorn
repoURL: https://charts.longhorn.io
version: "1.12.0"
namespace: longhorn-system
