apiVersion: cilium.io/v2
kind: CiliumBGPAdvertisement
metadata:
  name: unraid-lab-lan
  labels:
    advertise: unraid-lab-lan
spec:
  advertisements:
    - advertisementType: LBServiceAddress
      selector:
        matchLabels:
          syscode.dev/lan-vip: "true"
