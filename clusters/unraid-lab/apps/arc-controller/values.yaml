# ARC controller for GitHub Actions runner scale sets.
# The Helm release name is pinned to "arc" so runner scale-set values can bind
# to the stable service account: arc-gha-runner-scale-set-controller.

metrics:
  controllerManagerAddr: ":8080"
  listenerAddr: ":8080"
  listenerEndpoint: "/metrics"
