apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: imp
  namespace: argocd
  labels:
    syscode.io/cluster: oci-lab
  annotations:
    argocd.argoproj.io/sync-wave: "2"
    # Argo's structured-merge diff cannot decode Kubernetes 1.36 Deployment
    # status. Use the supported server-side dry-run diff for this application.
    argocd.argoproj.io/compare-options: ServerSideDiff=true
spec:
  project: default
  sources:
    - repoURL: https://github.com/syscode-labs/imp.git
      targetRevision: v0.8.0 # memory-pressure guardrails
      path: charts/imp
      helm:
        releaseName: imp
        valueFiles:
          - $values/values/clusters/oci-lab/imp.yaml
    - repoURL: https://github.com/syscode-labs/syscode-homelab-gitops-apps.git
      targetRevision: HEAD
      ref: values
  destination:
    name: oci-lab
    namespace: imp-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
