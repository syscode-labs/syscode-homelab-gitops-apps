apiVersion: k8s.bitwarden.com/v1
kind: BitwardenSecret
metadata:
  name: oci-pivot-controller-config
  namespace: oci-pivot-system
spec:
  organizationId: "28d10c96-b6fb-4e33-b486-b1f700017837"
  projectId: "306ed959-eb18-49e0-a9cf-b4a10008446b"
  secretName: oci-pivot-controller-config
  authToken:
    secretName: bw-auth-token
    secretKey: token
  map:
    - bwSecretId: "e2432bdb-b635-4049-94dd-b4a100e69991" # gitleaks:allow -- Bitwarden secret UUID, not a credential
      secretKeyName: OCI_COMPARTMENT_ID
