# bitwarden-sdk-server: companion REST wrapper for Bitwarden's Rust SDK —
# ESO's Go binary can't link it directly. TLS is required (chart default);
# the cert comes from bitwarden-sdk-server-tls (a cert-manager self-signed
# Certificate), not from this chart.
bitwarden-sdk-server:
  enabled: true
