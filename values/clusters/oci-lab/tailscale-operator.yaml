# The operator retains its owner identity; OCI Argo egress proxies use their
# dedicated, least-privilege identity.
operatorConfig:
  defaultTags: "tag:k8s-operator"
proxyConfig:
  defaultTags: "tag:oci-argo-egress"
