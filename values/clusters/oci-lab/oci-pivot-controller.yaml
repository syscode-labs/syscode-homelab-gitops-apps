# compartmentId comes from Bitwarden Secrets Manager via bitwarden-sm-operator
# (see clusters/oci-lab/apps/oci-pivot-secrets) as OCI_COMPARTMENT_ID, not a
# plain Helm value — was committed here once, had to be scrubbed from git
# history. Never put the real OCID in this file again.
extraEnvFrom:
  - secretRef:
      name: oci-pivot-controller-config
