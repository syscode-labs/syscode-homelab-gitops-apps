# CompartmentId is the OCID of the "syscode-homelab" compartment, verified
# via `oci --profile syscode-labs compute instance list` (hosts
# oci-talos-cp-1, oci-talos-worker-1, oci-bastion-01, oci-micro-01).
oci:
  compartmentId: "ocid1.compartment.oc1..REDACTED"
